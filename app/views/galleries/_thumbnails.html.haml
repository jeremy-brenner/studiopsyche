- if current_gallery
  %ul.thumbnails
    - current_gallery.pieces.each do |piece|
      %li
        = link_to piece, class: "thumbnail #{ @piece == piece ? 'active' : '' }" do
          = image_tag piece.image.url(:thumb) 